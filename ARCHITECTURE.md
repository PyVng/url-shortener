# Обзор архитектуры MVP URL Shortener

## Общая архитектура

Приложение URL Shortener представляет собой минимальный жизнеспособный продукт (MVP) для сокращения длинных URL-адресов в короткие ссылки. Архитектура построена на принципах простоты, масштабируемости и надежности.

## Компоненты системы

### 1. Backend (Flask API)

**Основной файл:** `main.py`

- **Фреймворк:** Flask с поддержкой CORS
- **Архитектура:** REST API с разделением на маршруты
- **Ленивая инициализация:** База данных инициализируется при первом запросе

**Ключевые эндпоинты:**
- `GET /` - обслуживание главной HTML страницы
- `POST /api/shorten` - создание короткого URL
- `GET /api/info/<short_code>` - получение информации о коротком URL
- `GET /<short_code>` - редирект на оригинальный URL (с поддержкой правил маршрутизации)
- `GET /api/version` - информация о версии приложения
- `GET /api/analytics/<short_code>` - детальная аналитика кликов (новое)
- `POST /api/rules` - управление правилами маршрутизации (новое)

### 2. Модели данных (SQLAlchemy)

**Основной файл:** `models.py`

- **ORM:** SQLAlchemy с декларативным стилем
- **Основная модель:** `Url` с полями:
  - `id` - первичный ключ
  - `short_code` - уникальный короткий код (6 символов)
  - `original_url` - оригинальный URL
  - `user_id` - идентификатор пользователя (опционально)
  - `title` - заголовок (опционально)
  - `click_count` - счетчик кликов
  - `created_at` - дата создания

**Бизнес-логика:**
- Генерация случайных коротких кодов
- Валидация URL (проверка протокола, длины)
- Увеличение счетчика кликов при редиректе

### 3. Схемы API (Pydantic)

**Основной файл:** `schemas.py`

- **Валидация:** Pydantic модели для входящих и исходящих данных
- **Схемы:**
  - `UrlCreate` - для создания короткого URL
  - `UrlResponse` - для ответа API
  - `UrlInfo` - для информации о URL

### 4. Управление базой данных

**Основной файл:** `database.py`

- **Поддержка нескольких СУБД:**
  - SQLite для локальной разработки
  - PostgreSQL для продакшена (Vercel/Render)
- **Ленивая инициализация:** Engine создается при первом обращении
- **Session management:** SQLAlchemy сессии с автоматическим закрытием

### 5. Frontend (Flask + Jinja2 + HTMX + Tailwind CSS)

**Основные файлы:**
- `templates/base.html` - базовый layout
- `templates/home.html` - главная страница
- `templates/my_links.html` - страница ссылок пользователя
- `templates/url_info.html` - информация о ссылке
- `templates/login.html` - страница входа
- `templates/register.html` - страница регистрации
- `templates/profile.html` - профиль пользователя

**Технологии:**
- **Flask Templates:** Jinja2 для серверного рендеринга
- **HTMX:** Прогрессивное улучшение для динамических взаимодействий
- **Tailwind CSS:** Утилитарный CSS фреймворк
- **Модульная архитектура:** Переиспользуемые компоненты

**Архитектура:**
- **Серверный рендеринг:** Страницы генерируются на сервере
- **HTMX интеграция:** AJAX запросы без JavaScript фреймворков
- **Компонентная структура:** Шаблоны разбиты на переиспользуемые компоненты
- **Отзывчивый дизайн:** Tailwind CSS с кастомными дизайн-токенами

**Ключевые компоненты:**
- `navbar.html` - навигационная панель с аутентификацией
- `auth_modal.html` - модальное окно входа/регистрации
- `url_form.html` - форма сокращения URL с HTMX
- `result_card.html` - карточка успешного результата
- `error_card.html` - карточка ошибки
- `links_grid.html` - сетка ссылок пользователя
- `link_card.html` - отдельная карточка ссылки
- `empty_state.html` - состояние "пусто"

### 6. Кэширование (Redis)

**Основной файл:** `cache.py`

- **Цель:** Кэширование URL данных для повышения производительности
- **TTL:** Настраиваемый time-to-live (по умолчанию 1 час)
- **Функции:**
  - Кэширование данных URL
  - Инвалидация кэша при изменениях
  - Статистика использования кэша
  - Счетчики для A/B тестирования

### 7. Фоновые задачи (Celery)

**Основные файлы:** `celery_app.py`, `tasks.py`

- **Брокер:** Redis для очередей задач
- **Backend:** Redis для хранения результатов
- **Очереди:**
  - `visits` - логирование посещений
  - `analytics` - обработка аналитики
  - `default` - общие задачи
- **Ключевые задачи:**
  - `log_visit` - асинхронное логирование кликов с аналитикой
  - `process_analytics` - обработка аналитических данных
  - `cleanup_old_visits` - очистка старых записей

### 8. Расширенные модели данных

**Дополнительные модели в `models.py`:**

- **Rule модель:** Правила условных редиректов
  - `rule_type` - тип правила (country, device, referrer, time, weight)
  - `condition_value` - условие применения правила
  - `target_url` - целевой URL для редиректа
  - `weight` - вес для A/B тестирования
  - `priority` - приоритет правила

- **Visit модель:** Детальная аналитика посещений
  - `ip_address` - IP адрес посетителя
  - `user_agent` - строка User-Agent
  - `country_code` - код страны (ISO 3166-1 alpha-2)
  - `device_type` - тип устройства (mobile/tablet/desktop)
  - `browser` - браузер
  - `os_name` - операционная система
  - `referrer` - источник перехода
  - `final_url` - итоговый URL редиректа

### 9. Аналитические библиотеки

**Внешние зависимости:**
- **geoip2:** Геолокация по IP адресу
- **user-agents:** Парсинг User-Agent строк
- **maxminddb:** Работа с GeoIP базами данных

**Функции:**
- Определение страны по IP
- Анализ типа устройства и браузера
- Парсинг реферера
- Обогащение данных аналитики

## Архитектурные решения

### 1. Выбор технологий

- **Flask:** Легковесный, гибкий, отличная поддержка REST API и шаблонов
- **Jinja2:** Мощный шаблонизатор для серверного рендеринга
- **HTMX:** Прогрессивное улучшение для динамических взаимодействий без JavaScript фреймворков
- **Tailwind CSS:** Утилитарный CSS фреймворк для быстрой разработки интерфейсов
- **SQLAlchemy:** Мощная ORM с поддержкой миграций
- **Pydantic:** Строгая валидация данных, автодокументация API

### 2. Масштабируемость

- **База данных:** Поддержка PostgreSQL для продакшена на Render
- **Кэширование:** ✅ Redis для кэширования URL данных и счетчиков
- **Фоновые задачи:** ✅ Celery для асинхронной обработки аналитики
- **CDN:** Статические файлы могут быть размещены на CDN
- **Горизонтальное масштабирование:** Redis позволяет масштабировать кэширование

### 3. Безопасность

- **Валидация URL:** Проверка протокола и длины
- **CORS:** Настроен для кросс-доменных запросов
- **SQL Injection:** Предотвращено через SQLAlchemy ORM

### 4. Мониторинг и аналитика

- **Счетчик кликов:** ✅ Асинхронное логирование через Celery
- **Детальная аналитика:** ✅ Логирование IP, User-Agent, геолокации, устройств
- **Кэширование:** ✅ Redis для быстрого доступа к данным
- **SLA мониторинг:** Инфраструктура для мониторинга производительности (<50мс)
- **Логирование:** Базовое логирование ошибок + аналитика посещений
- **Версионирование:** API с информацией о версии

### 5. Продвинутые функции (расширенная архитектура)

**Инфраструктура готова для следующих функций:**

- **🗺️ Умная маршрутизация:** Условные редиректы по гео/устройству/рефереру/времени
  - Франция → french.example.com
  - Мобильные → m.example.com
  - Вечером → promo.example.com

- **⚖️ A/B тестирование:** Распределение трафика по вариантам с весами
  - 50/50, 30/70 распределение
  - Тестирование эффективности ссылок

- **📊 Детальная аналитика:** Логирование каждого клика с метаданными
  - Геолокация, устройства, источники, время
  - Страны, браузеры, ОС, рефереры

- **⚡ SLA мониторинг:** Автоматический контроль производительности
  - P95 задержки <50мс
  - Cache hit ratio, ошибки
  - Redis статистика

### 6. Объем MVP

MVP реализует базовый функционал сокращения URL с инфраструктурой для продвинутых возможностей:

- **Маршрутизация:** ✅ Простой редирект + инфраструктура для умной маршрутизации
- **A/B тестирование:** ✅ Модели и веса готовы для реализации
- **Правила маршрутизации:** ✅ Rule модель для условных редиректов
- **Асинхронное логирование:** ✅ Celery для фоновой обработки аналитики
- **Аналитика:** ✅ Детальная статистика (IP, гео, устройство, браузер)
- **Кэширование:** ✅ Redis для производительности и SLA <50мс
- **Дашборд:** Отдельный дашборд не требуется, достаточно API + существующего frontend

## Потенциальные улучшения

### Уже реализовано (инфраструктура готова)
- ✅ **Добавление аутентификации пользователей** - реализовано (JWT токены, регистрация/логин)
- ✅ **Кэширование с Redis** - реализовано для URL данных и счетчиков
- ✅ **Асинхронная обработка** - Celery для фоновых задач аналитики
- ✅ **Детальная аналитика** - логирование IP, гео, устройств, браузеров
- ✅ **Умная маршрутизация** - инфраструктура для правил редиректов
- ✅ **A/B тестирование** - модели с весами готовы
- ✅ **SLA мониторинг** - Redis для производительности <50мс

### Краткосрочные (можно добавить)
- Кастомные короткие коды
- QR-коды для коротких ссылок
- Активация правил маршрутизации (логика применения)
- Веб-интерфейс для управления правилами
- Дашборд аналитики с графиками

### Долгосрочные
- Микросервисная архитектура
- Интеграция с внешними сервисами (Google Analytics, etc.)
- Расширенное A/B тестирование с статистикой
- Машинное обучение для оптимизации редиректов
- Multi-region deployment с геораспределением

## Деплоймент

- **Локальная разработка:** SQLite + Flask dev server
- **Продакшен:** PostgreSQL + Vercel/Render
- **CI/CD:** Автоматическое развертывание при пуше в main

## Тестирование

- **Unit тесты:** Модели, API эндпоинты
- **E2E тесты:** Playwright для пользовательских сценариев
- **Интеграционные тесты:** Проверка работы с базой данных
